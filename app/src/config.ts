// Point this at the laptop running `npm start` inside /server, using your
// laptop's LAN IP (not localhost — your phone can't reach your laptop's
// localhost). Find it with `ipconfig` (Windows) -> IPv4 Address.
//
// Example: "http://192.168.1.42:4000"
// Detected on this machine via `ipconfig` — verify it's your Wi-Fi adapter's
// IPv4 (not a VPN/virtual adapter) before relying on it.
export const API_BASE_URL = "http://10.68.10.44:4000";
